<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 72 72">
	<!-- Default key art for Run Action - matches the dark-background style used by the
	     dynamically-rendered Transport/Track icons (src/util/icons.ts) so the whole
	     plugin reads as one visual system. -->
	<rect width="72" height="72" fill="#1e1e1e" />
	<circle cx="36" cy="36" r="18" fill="none" stroke="#1C9E8E" stroke-width="5" />
	<circle cx="36" cy="36" r="6" fill="#1C9E8E" />
</svg>
